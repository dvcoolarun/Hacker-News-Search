import React from 'react';
import PropTypes from 'prop-types';

const PostListPage = ({ }) => {
    return(
        /* API CALLS FOR THE POST LIST WILL GO HERE. */
    );
};

PostListPage.propTypes = {
    
};

export default PostListPage;

