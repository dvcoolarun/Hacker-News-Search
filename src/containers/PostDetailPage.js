import React from 'react';
import PropTypes from 'prop-types';

const PostDetailPage = ({ }) => {
    return(
        /* API CALL HANDLING FOR THE POST DETAIL */
    );
};

PostDetailPage.propTypes = {
    
};

export default PostDetailPage;
